import React from 'react';
import { Typography } from '@mui/material';

const Profile = () => (
  <Typography variant="body1" style={{ padding: '6em 3em' }}>
    Welcome to the Uprising Dashboard
  </Typography>
);

export default Profile;
